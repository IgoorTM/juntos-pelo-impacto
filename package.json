{
  "name": "juntos-pelo-impacto",
  "version": "0.0.1",
  "private": true,
  "description": "Sistema de Gestão Juntos pelo Impacto — MVP",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace @juntos/frontend",
    "dev:backend": "npm run start:dev --workspace @juntos/backend --if-present",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:e2e": "npm run test:e2e --workspace @juntos/backend --if-present",
    "clean": "npm exec --workspaces -- rm -rf dist build .next",
    "validate": "npm run typecheck && npm run lint && npm run test && npm run test:e2e && npm run build",
    "validate:workspace": "npm run typecheck --workspace && npm run lint --workspace && npm run test --workspace && npm run test:e2e --workspace --if-present && npm run build --workspace",
    "docker:db": "docker compose up -d postgres",
    "docker:db:down": "docker compose stop postgres",
    "docker:dev": "docker compose up",
    "docker:dev:build": "docker compose up --build",
    "docker:prod": "docker compose -f docker-compose.prod.yml up -d",
    "docker:prod:build": "docker compose -f docker-compose.prod.yml up -d --build",
    "docker:down": "docker compose down",
    "docker:down:volumes": "docker compose down -v",
    "docker:logs": "docker compose logs -f"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
